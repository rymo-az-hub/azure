# Legacy Key Vault template (archived)

This directory contains an earlier Key Vault template that was used during initial iterations.

**Canonical templates are under:**
- `infra/kv/pe-v1/` (Key Vault + Private Endpoint baseline)
- `infra/kv/rbac-v2/` (RBAC v2 minimum privilege)

## Notes
- Keep this only as a reference. Do not extend this directory going forward.
